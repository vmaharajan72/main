
# V Maharajan — GitHub Pages Site

A minimal, fast, responsive portfolio/blog template built with plain HTML/CSS/JS.

## 🚀 Deploy
1. Create a public repo named `YOUR-USERNAME.github.io`.
2. Add these files to the repo and commit.
3. In **Settings → Pages**, select `main` as source. Wait ~1 minute.
4. Your site will be live at `https://YOUR-USERNAME.github.io`.

## 🔧 Customize
- Update titles, meta tags, and content in `index.html`.
- Replace `assets/avatar.jpg` and `assets/favicon.png`.
- Edit styles in `style.css` (colors, spacing, grids).
- Add posts as static cards under the **Blog** section (or migrate to Jekyll later).

## 🌐 Optional: Custom Domain
- Create a `CNAME` file with your domain:  
  `example.com`
- In your DNS, point `example.com` and/or `www.example.com` to `YOUR-USERNAME.github.io`.
- Enable HTTPS in **Settings → Pages**.

## 📈 Analytics (Optional)
- Add your analytics script (e.g., Plausible) before `</head>` in `index.html`.

## 🛡️ License
MIT — do anything, attribution appreciated.
